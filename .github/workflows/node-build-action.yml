name: npm build

on:
  workflow_call:

jobs:
  node-build:
    runs-on: ubuntu-22.04
    steps:
      - name: node setup
        uses: actions/setup-node@v4
        with:
          node-version: 18
        run: ls -lha 
        run: npm ci
        run: npm test
    